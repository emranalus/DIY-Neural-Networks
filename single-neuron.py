# Single Neuron

# 3 inputs
inputs = [1, 2, 3]

# Parameters
weigths = [2, 1.8, -0.5]
bias = 1

# Single Output
output = (inputs[0] * weigths[0] + 
          inputs[1] * weigths[1] +
          inputs[2] * weigths[2] + bias)

print(output)

